export enum IconsEnum {
  checkCircleIcon = "fa fa-check-circle",
  editIcon = "fa fa-edit",
  deleteIcon = "fa fa-trash",
  smile = "fa fa-smile-o",
}

export enum AddButtonEnum {
  OK = "Ok",
  ADD = "Add",
}

export enum ModalButtonTextEnum {
  CANCEL = "Cancel",
  YES = "Yes",
}

export enum ButtonTextEnum {
  DELETE = "Delete",
  EDIT = "Edit",
  COMPLETED = "Completed",
}

export enum EmptyToDoList {
  EMPTY_TODO_LIST = "You have no tasks add tasks ",
}

export enum ModalTextEnum {
  MODAL_TEXT = "Are you sure you want to delete  your task?",
}
